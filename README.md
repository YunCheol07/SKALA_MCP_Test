# SKALA_MCP_Test
팀 프로젝트 진행 전에 MCP 한번 연습해보기 (A.X 4.0 Light 모델이 무거워서 현재 노트북에선 안돌아감 수정이 필요할듯)

# A.X 4.0 Light MCP 앱 (텍스트 전용)

SK텔레콤의 A.X 4.0 Light 모델을 사용하는 MCP (Model Context Protocol) 애플리케이션입니다.

## 📋 프로젝트 구조

```
mcp-app/
├── config.py           # 설정 파일
├── llm_handler.py      # A.X 4.0 VL Light 모델 핸들러
├── app.py              # MCP 클라이언트 앱
├── main.py             # 메인 진입점
├── requirements.txt    # 의존성 패키지
├── .env.example        # 환경 변수 예제
├── .gitignore         # Git 무시 파일
├── config/            # 설정 파일들
├── models/            # 모델 캐시 디렉토리
├── utils/             # 유틸리티 함수들
└── tests/             # 테스트 코드
```

## 🚀 시작하기

### 1. 가상환경 설정

```powershell
# 가상환경 생성
python -m venv venv

# 가상환경 활성화
.\venv\Scripts\activate
```

### 2. 패키지 설치

```powershell
pip install -r requirements.txt
```

**참고**: CPU 전용 PyTorch가 자동으로 설치됩니다.

### 3. 환경 변수 설정

```powershell
# .env.example을 .env로 복사
copy .env.example .env

# .env 파일을 편집하여 필요한 값 설정
```

### 4. 실행

```powershell
# 메인 앱 실행
python main.py

# 또는 직접 app.py 실행
python app.py
```

### 5. LLM 핸들러 테스트

```powershell
# LLM 모델만 테스트
python llm_handler.py
```

## 💡 사용 방법

### 텍스트 질문

```
👤 You: 인공지능에 대해 설명해줘
🤖 Assistant: [응답]
```

### 종료

```
👤 You: quit
```

## 🔧 설정

`config.py`에서 다음을 수정할 수 있습니다:

- `MODEL_NAME`: 사용할 모델 이름
- `GENERATION_CONFIG`: 텍스트 생성 파라미터
- `USE_GPU`: GPU 사용 여부
- `USE_DIRECTOR`: Director 프록시 사용 여부

## 📊 시스템 요구사항

### 최소 사양
- **RAM**: 16GB
- **저장공간**: 20GB (모델 캐시 포함)
- **Python**: 3.8 이상
- **CPU**: 멀티코어 권장

### 권장 사양 (현재 환경 - CPU 전용)
- **RAM**: 32GB ✅
- **CPU**: 최신 멀티코어 프로세서
- **저장공간**: 50GB 이상
- **Python**: 3.10 이상

**참고**: 
- 이 프로젝트는 CPU 전용으로 설정되어 있습니다 (내장 그래픽 환경)
- GPU가 있어도 CPU 모드로 실행됩니다
- 응답 생성 시간: 약 30초~1분 예상

## 🎯 다음 단계

현재는 MCP 클라이언트(앱)만 구현되어 있습니다. 다음 단계로:

1. ✅ MCP 앱 구현 (완료)
2. ⏳ MCP 서버 구현 (예정)
3. ⏳ Director 통합 (선택사항)

## 📝 모델 정보

**A.X 4.0 Light (텍스트 전용)**
- 개발사: SK텔레콤
- 파라미터: 7B (경량 모델)
- 특징: 한국어 텍스트 이해 최적화
- 라이선스: Apache 2.0
- HuggingFace: https://huggingface.co/skt/A.X-4.0-Light

**참고**: 이미지 처리가 필요한 경우 A.X 4.0 VL Light 모델로 업그레이드 가능합니다.

## 🐛 문제 해결

### 응답 생성이 너무 느림
```python
# config.py에서 토큰 수 줄이기
GENERATION_CONFIG = {
    "max_new_tokens": 128,  # 256에서 128로 감소
    ...
}
```

### 메모리 부족
- 다른 프로그램 종료
- `low_cpu_mem_usage=True` 설정 확인 (이미 적용됨)

### 모델 다운로드 실패
- 인터넷 연결 확인
- 허깅페이스 토큰 설정 (private 모델인 경우)
- `cache_dir` 경로 확인

## 📄 라이선스

이 프로젝트는 MIT 라이선스를 따릅니다.

A.X 4.0 VL Light 모델은 Apache 2.0 라이선스를 따릅니다.